<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/movies/actorForFilm/<%= pelicula.id %>" method="POST">
        <label><%= pelicula.title %></label>
        <label for="">Genero</label>
        <select name="actor">
            <% for(i=0; i < actor.length; i++) {%>
                <option value="<%= actor[i].id %>">
                    <%= actor[i].first_name %> <%= actor[i].last_name %>
                </option>
            <% } %>
            <input type="submit">
        </select>
    </form>
</div>
</body>
</html>