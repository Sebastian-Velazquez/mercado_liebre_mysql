/* let db = require("../../database/models");

const controlador ={ 
    list: (req, res)=>{
        //res.send("estamos en lista")
        db.Peliculas.findAll().then(function(peliculas){
            res.render("moviesList",{peliculas:peliculas})
        })
    }


}

module.exports = controlador; */